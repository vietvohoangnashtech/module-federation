import React from 'react';
import {Spinner} from '../src/components/Spinner';

export default {
  title: 'Core/Spinner',
  component: Spinner,
};

export const Basic = () => <Spinner animation='border' />;
